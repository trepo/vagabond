'use strict';

/**
 * The direction of an edge.
 *
 * Node --OUT--> Edge --IN--> Node
 *
 * Possible Values:
 * * IN
 * * OUT
 * * BOTH
 * @type {Object}
 */
let Direction = {
  IN: 'IN',
  OUT: 'OUT',
  BOTH: 'BOTH'
};

export default Direction;
